---
title: Using Components
description: Learn about the interactive React components available in the framework.
keywords: [components, react, interactive]
last_update:
  date: 02/20/2026
  author: Trellis
---

The Trellis framework includes several reusable React components you can use in your MDX files.

## Design Tokens

The framework uses a design token system that converts a JSON file (`design-tokens.json`) into CSS custom properties at build time.

```json title="design-tokens.json (excerpt)"
{
  "brand": {
    "primary-600": {
      "value": "#2a5a82",
      "type": "color"
    }
  },
  "accent": {
    "olive-400": {
      "value": "#b4ca4a",
      "type": "color"
    }
  }
}
```

These become CSS variables like `--color-brand-primary-600` and `--color-accent-olive-400` that you can reference throughout your styles.

## Theme Customizations

### Last Updated at Top

The last-updated date and edit links appear **at the top** of each page, right below the title. This gives readers immediate context about content freshness.

### Heading Anchors

Hover over any heading to see a link icon. Click it to copy the anchor URL to your clipboard.

### Tab URL Sync

When you switch tabs, the selection is synced to the URL query parameters. This means you can share a link that opens on the correct tab.

## Custom Admonition Icons

The framework includes custom SVG icons for each admonition type (note, tip, info, caution, danger) that provide a cleaner visual experience than the defaults.

## Plugins

### Smart Search

The search plugin indexes all documentation at build time and provides a fast, client-side search experience. Configure weights in `config/site.ts`:

```js
{
  search: {
    weights: {
      title: 1.0,
      'sections.heading': 1.0,
      keywords: 0.8,
      description: 0.6,
      'sections.content': 0.5,
      content: 0.4,
    }
  }
}
```

### Redirects

Manage URL redirects via `redirects.json`:

```json
[
  { "from": "/old-page/", "to": "/new-page/" }
]
```

### Image Lightbox

All images in markdown content automatically support click-to-zoom via the lightbox plugin. No additional markup needed.
